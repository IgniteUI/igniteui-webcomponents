import type { Themes } from '#theming/types.js';

// The component has no schema in `igniteui-theming` yet; its variables are
// declared in `shared/speech-to-text.common.scss` for every theme.
export const all: Themes = { light: {}, dark: {} };
