{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "npm run build.core && npm run start.app",
    "build.core": "lerna run --scope='igniteui-webcomponents' build --stream",
    "start.app": "lerna run --scope='app' start --stream",
    "test": "lerna run --scope='igniteui-webcomponents' test --stream",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
